@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "二要素認証（2FA）";
}

<h1 class="text-light">@ViewData["Title"]</h1>
<hr />
<p class="text-light">二要素認証（2FA）が有効になっています。 認証アプリの認証コードを入力してください。</p>
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="ModelOnly" class="alert alert-danger text-danger ps-0" role="alert"></div>
            <div class="form-floating">
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                <label asp-for="Input.TwoFactorCode" class="form-label"></label>
                <span asp-validation-for="Input.TwoFactorCode" class="text-warning"></span>
            </div>
            <div>
                <div class="checkbox">
                    <label asp-for="Input.RememberMachine" class="form-label">
                        <input asp-for="Input.RememberMachine" />
                        @Html.DisplayNameFor(m => m.Input.RememberMachine)
                    </label>
                </div>
            </div>
            <div>
                <button type="submit" class="w-100 btn btn-lg btn-primary">ログイン</button>
            </div>
        </form>
    </div>
</div>
<p class="text-light">
    認証デバイスを使用できない場合は 
    <a class="text-light" id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">リカバリコードでログイン</a>できます。
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}