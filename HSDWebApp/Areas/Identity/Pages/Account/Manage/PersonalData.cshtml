@page
@model PersonalDataModel
@{
    ViewData["Title"] = "個人情報";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3 class="text-light">@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p class="text-light">
            アカウントには本サービスへ登録された個人情報が存在しています。<br>
            このページでは個人情報のダウンロードと削除が行えます。
        </p>
        <p class="text-light">
            <strong>
                個人情報を削除する場合、アカウントを削除します。<br />
                削除されたアカウントやデータは復元できません。
            </strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post">
            <button class="btn btn-primary" type="submit">ダウンロード</button>
        </form>
        <p>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger">削除</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
